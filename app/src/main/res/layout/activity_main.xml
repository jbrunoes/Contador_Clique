<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Contar"
        android:id="@+id/button"
        android:onClick="contador"
        android:layout_below="@+id/recebedor"
        android:layout_toLeftOf="@+id/resetar"
        android:layout_toStartOf="@+id/resetar"
        android:layout_marginTop="74dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/recebedor"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:singleLine="false"
        android:textColor="#192eff"
        android:textSize="@android:dimen/app_icon_size" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reiniciar"
        android:id="@+id/resetar"
        android:onClick="resetar"
        android:layout_alignTop="@+id/button"
        android:layout_toRightOf="@+id/recebedor"
        android:layout_toEndOf="@+id/recebedor" />

</RelativeLayout>
